<!--
-&#45;&#45;
import type { CollectionEntry } from "astro:content";

export interface Props {
  page: any;
}

const { page } = Astro.props as { pages: CollectionEntry<"Impressum"> };
const { Content } = await page.render();
-&#45;&#45;

<section id={page.title}>
  <h1>{page.title}</h1>
  <article>
    <Content />
  </article>
  <div></div>
</section>

<style lang="scss">
  section {
    width: 100%;
    height: 100vh;
    background: white;
  }
</style>
-->
