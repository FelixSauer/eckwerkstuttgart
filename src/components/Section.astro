---
export interface Props {
	page: any;
}
const {page} = Astro.props;
const {Content} = await page.render();
---

<section id={page.data.title}>
    <div>
        <article>
            <div>
                <Content/>
            </div>

        </article>
    </div>
</section>

<!--
{page.data.title === "Home" && (
<Gallery imageCollection={page.data.title.toString()}/>
	)}
-->
