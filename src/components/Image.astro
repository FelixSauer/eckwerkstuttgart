<!--
-&#45;&#45;
import { Image } from "astro:assets";

interface Props {
  src: string;
  altText: string;
}

const src = Astro.props.src;
const altText = Astro.props.altText;
-&#45;&#45;

<AstroImage src={src} alt={altText} />

<style lang="scss">
  div {
    display: flex;
    justify-content: center;
    align-items: center;

    img {
      width: 100%;
      height: auto;
    }
  }
</style>
-->
